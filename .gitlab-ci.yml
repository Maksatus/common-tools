# Публикует папку public/ на GitLab Pages при каждом пуше в основную ветку.
pages:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Публикую public/"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
