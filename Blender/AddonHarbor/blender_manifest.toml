schema_version = "1.0.0"

id = "addon_harbor"
name = "Addon Harbor"
version = "0.2.1"
tagline = "One-click access to the private extension repository"
maintainer = "You <you@example.com>"
type = "add-on"

license = ["SPDX:GPL-3.0-or-later"]

blender_version_min = "4.2.0"

[permissions]
network = "Fetch the extension index from the private repository"
